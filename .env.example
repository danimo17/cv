# Copia a .env (ignorat per git). Clau de Giphy: https://developers.giphy.com (app tipus API).
# A producció (Cloudflare Workers) és un Secret amb el mateix nom. Mai al xat, mai a fitxers versionats.
NUXT_GIPHY_API_KEY=
