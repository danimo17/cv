# Backlog

| Slug                             | Story                                                                                                                                                                                                                                                                                                                        | Status | Folder                                                                                   |
| -------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ---------------------------------------------------------------------------------------- |
| bootstrap                        | Full CV website (home + meme) with governance, styles, gates, environments and deploy                                                                                                                                                                                                                                        | done   | merged to `main` (PR #1, `03eedd9`)                                                      |
| post-launch                      | `www` DNS, Dependabot fix (manual pendings); D1-D5 decided and done                                                                                                                                                                                                                                                          | done   | merged to `main` (PR #2, `6bf423b`)                                                      |
| english-only                     | Translate the whole repo to English (rule 13, decision 040): `.claude/` docs, `CLAUDE.md`, code comments                                                                                                                                                                                                                     | done   | `docs/decisions/040-english-only.md`                                                     |
| arch-no-comments-no-suppressions | Add `tests/arch/no-code-comments.spec.ts` and `tests/arch/no-lint-suppressions.spec.ts` to automate rules 14/15 (decision 041) — manual review for now                                                                                                                                                                       | todo   | `.claude/docs/decisions/041-code-comments-zero-suppressions-autoimport.md`               |
| require-contract-decision-check  | `require-contract.py`'s rule-11 check only hardcodes `nuxt.config.ts`/`eslint.config.mjs` — generalize to other structural config (`playwright.config.ts`, `vitest.config.ts`, `wrangler.jsonc`, `package.json` deps) and add a test for the hook itself (code review finding, `post-launch`)                                | todo   | `.claude/hooks/require-contract.py`                                                      |
| deploy-secrets-order             | Fix `deploy.yml` uploading Worker secrets before deploying (Cloudflare error 10215, PR #2's deploy silently failed); add mandatory post-merge deploy verification (decision 042)                                                                                                                                             | done   | merged to `main` (PR #3, `e96aa05`)                                                      |
| ux-redesign                      | Header/nav, locale-switcher dropdown, control heights, elevation system, timeline, CV content fixes (17 criteria)                                                                                                                                                                                                            | done   | merged to `main` (PR #9, `dd87dc0`); `.claude/tasks/closed/ux-redesign.md`               |
| spacing-tokens                   | Component CSS today picks its own raw `gap-`/`p-`/`m-` values per file; add 2 semantic spacing tokens (e.g. `--spacing-sm`/`--spacing-lg`, one changeable base behind them) and migrate every component CSS file to use only those, same pattern as the existing `shadow-neu-*`/`radius-*` tokens (user request, 2026-09-22) | todo   | none yet                                                                                 |
| workflow-integrity-hardening     | Close 5 process gaps found in one session: unmerged-branch detection before starting new work, synchronized cross-file changes (no orphaned test/e2e references), reusable-component change-scope confirmation, a mechanical rule-12 delegation check, and mechanizing every other hard rule still on "human review only"    | todo   | `.claude/tasks/workflow-integrity-hardening/story.md`                                    |
| css-tailwind-first               | Drop per-component `app/assets/css/components/*.css` + `@apply` in favour of Tailwind utilities directly in templates and computed per-prop class lists; real `.scss` only where Tailwind genuinely can't express something; reverses rule 08/decisions 026/038 — needs a decision record first                              | todo   | `.claude/tasks/css-tailwind-first/story.md`                                              |
| task-close-gate                  | `require-contract.py`: empty `ACTIVE` allowed for docs-only commits; closing a task requires a fully-checked `.claude/tasks/closed/<slug>.md` archive in the same commit (decision 051)                                                                                                                                      | done   | merged to `main` (PR #10, `3c52325`); `.claude/tasks/closed/task-close-gate.md`          |
| dependency-prs-triage            | 5 open Dependabot PRs (#4-8) investigated empirically in isolated worktrees against current `main` — all safe, all merged                                                                                                                                                                                                    | done   | merged to `main` (PRs #4-8)                                                              |
| button-system                    | `CustomButton` redesign (icon mandatory, tooltip text, async-action loading spinner), new `ButtonCollection` for spacing, and a -1/0/1 elevation model for buttons vs. inputs — grill-me started, interrupted after Q1                                                                                                       | todo   | `.claude/tasks/button-system/story.md`                                                   |
| push-review-gate                 | `require-contract.py`: `git push` on a `feat/*` branch with an active task requires a fully-checked `## Review` section (checklist + `/code-review` outcome) in `handoff.md` before it's allowed (decision 054) — hard block, no escape hatch                                                                                | done   | merged to `main` (PR #12, `3077327`); `.claude/tasks/closed/push-review-gate.md`         |
| composable-store-regions         | Fixed `// #region` structure for composables (Static vars → Reactive vars → Methods → Component data → Lifecycle) and stores (State → Getters → Methods), rule 14 exception for exactly these markers (decision 052)                                                                                                         | done   | merged to `main` (PR #13, `115e2be`); `.claude/tasks/closed/composable-store-regions.md` |
