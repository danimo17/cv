# Contract: Post-launch fixes

**Story:** `story.md` · **Declared data sources (rule 02):** none (only our own infrastructure and GitHub/Cloudflare logs).

## Acceptance criteria

| #   | Given / When / Then                                                                                                                                                                                                                                                                                                                           | Test that covers it                                                                                                                                                                                                       |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Given `danimorales.dev`, When an HTTPS request is made, Then it responds 200 with the home page                                                                                                                                                                                                                                               | ✅ done — `curl -I https://danimorales.dev` → 200                                                                                                                                                                         |
| 2   | Given `www.danimorales.dev`, When an HTTPS request is made, Then it responds (redirect or same app)                                                                                                                                                                                                                                           | ✅ done — Custom Domain added to the `cv` Worker; `curl` with `--resolve` to Cloudflare's IP → 200 (local DNS still caches the previous NXDOMAIN, normal propagation)                                                     |
| 3   | Given the Dependabot job "npm_and_yarn Update #2", When it runs again, Then it does not fail                                                                                                                                                                                                                                                  | ✅ done — `packageManager` downgraded to `pnpm@11.17.0` (avoids the native-binary auto-download that failed in the Dependabot sandbox); `pnpm install --frozen-lockfile` + `pnpm gate` green locally with the new version |
| 4   | Given the D1-D5 points from the bootstrap handoff, Then each one has a decision in `.claude/docs/decisions/` or is marked as dropped                                                                                                                                                                                                          | human review (done: decisions 034-038)                                                                                                                                                                                    |
| 5   | Given the `App*` prefix (decision 038), When the whole family is renamed to `Custom*`, Then no old reference remains in code, docs, ESLint or tests, and the gate stays green                                                                                                                                                                 | ✅ done — `pnpm gate`, `tests/arch/css-per-component.spec.ts`, `App[A-Z]` grep clean                                                                                                                                      |
| 6   | Given `/meme`, When at least one term has been searched, Then there is a "recent searches" section with at most 5 terms, clickable, which fill the field and run the search                                                                                                                                                                   | ✅ done — `tests/unit/stores/giphy.spec.ts`, `MemeRecentSearches.spec.ts`; verified live (`pnpm dev`, cat→dog→click "dog" refills the field and searches again)                                                           |
| 7   | Given paginable Giphy results, When there is more than one page, Then a generic `CustomPagination` component allows navigating them from `/meme`                                                                                                                                                                                              | ✅ done — `CustomPagination.spec.ts`; verified live (Next page → "Page 2 of 42" with new results)                                                                                                                         |
| 8   | Given `CustomImage` (decision 037), When the optional `srcset`/`sizes` props are passed, Then they are reflected on the `<img>`; if not passed, behavior identical to current                                                                                                                                                                 | ✅ done — `tests/unit/components/CustomImage.spec.ts`                                                                                                                                                                     |
| 9   | Given rule 13 (decision 040), When the whole repo is scanned for Catalan characters outside `i18n/locales/`, Then none remain except data proper nouns (company/place names in `app/data/cv/{experience,education}.ts`)                                                                                                                       | ✅ done — `grep -rlIE '[àèéíòóúçïü]' .claude app server shared tests e2e CLAUDE.md` clean except those 2 data files; `pnpm gate` green after                                                                              |
| 10  | Given rule 14 (decision 041), When `app/`, `server/`, `shared/` and root config files are scanned, Then no `//`, `/** */` or `<!-- -->` comments remain outside `tests/**`/`e2e/**`                                                                                                                                                           | ✅ done — 5 parallel subagents by directory + main thread for `eslint.config.mjs`/`playwright.config.ts`; `pnpm gate` green after (270/270 tests, 0 lint/typecheck errors, 1 accepted warning)                            |
| 11  | Given rule 15 (decision 041), When `pnpm gate` runs, Then no `eslint-disable*`/`@ts-expect-error`/`@ts-ignore` exists anywhere in the repo                                                                                                                                                                                                    | ✅ done — the one instance (`CustomInput.vue`) removed, `vue/require-default-prop` left as an accepted warning (see decision 041)                                                                                         |
| 12  | Given decision 041 (auto-import), When `app/domain`, `app/services`, `app/data`, `app/ui-config`, `app/types` symbols are used elsewhere, Then no explicit `~/...` import is needed for them (except type-only imports Nuxt's auto-import can't reach); `@fortawesome/*` stays explicit-only in `CustomIcon.vue`/`app/plugins/fontawesome.ts` | ✅ done — `nuxt.config.ts` `imports.dirs`; `pnpm typecheck` green with explicit imports removed                                                                                                                           |

## Standards to consult

- `.claude/docs/decisions/024-github-environments-and-branch-protection.md` (Cloudflare token permissions)
- `.claude/docs/catalog/ai-workflow.md` (secrets and deploy table)
- `.claude/docs/decisions/026-custom-primitives.md` + `038-rename-app-to-custom-prefix.md` (rename)
- `.claude/docs/decisions/034-meme-recent-searches-and-pagination.md`, `037-customimage-srcset-ready.md`
- `.claude/docs/standards/{components,state,i18n,testing}.md`

## Open questions

- None. D1-D5 decided (034-038). Dependabot: the user chose to downgrade `packageManager` (vs. waiting for the
  automatic retry) — applied.
